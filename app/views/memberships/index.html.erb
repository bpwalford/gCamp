<div class="members-page">

  <div class="page-header">
    <h1><%= @project.name %>: Manage Members</h1>
  </div>

  <%= if current_user.owner?(@project)
        render 'owner_index.html.erb'
      else
        render 'member_index.html.erb'
      end %>

</div>
