<%= form_for(@user, url: register_path) do |f| %>


  <div class="col-md-4 col-md-offset-4 well">

    <h3>Sign up for gCamp</h3><br>

    <% if @user.errors.any? %>
      <div class="alert alert-danger">
        <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field form-group">
      <%= f.label :first_name %>
      <%= f.text_field :first_name, class: "col-md-12" %>
    </div>

    <div class="field form-group">
      <%= f.label :last_name %>
      <%= f.text_field :last_name, class: "col-md-12" %>
    </div>

    <div class="field form-group">
      <%= f.label :email %>
      <%= f.email_field :email, class: "col-md-12" %>
    </div>

    <div class="field form-group">
      <%= f.label :password %>
      <%= f.password_field :password, class: "col-md-12" %>
    </div>

    <div class="field form-group">
      <%= f.label :password_confirmation %>
      <%= f.password_field :password_confirmation, class: "col-md-12" %>
    </div>

    <br>
    
    <div class="actions form-group">
      <%= f.submit "Sign Up", class: "btn btn-primary" %>
    </div>

  </div>

<% end %>
